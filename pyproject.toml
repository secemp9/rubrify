[project]
name = "rubrify"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "harnify-ai",
    "harnify-agent",
    "pydantic>=2.10",
    "defusedxml>=0.7",
]

[project.optional-dependencies]
evolve = ["gepa>=0.1.0"]

[tool.uv.sources]
harnify-ai = { workspace = true }
harnify-agent = { workspace = true }
gepa = { path = "../../../gepa", editable = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rubrify"]
